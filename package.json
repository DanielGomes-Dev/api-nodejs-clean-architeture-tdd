{
  "name": "api-nodejs-clean-architecture-tdd",
  "version": "1.0.0",
  "description": "",
  "main": "src/main/index.js",
  "scripts": {
    "start": "node src/main",
    "test": "jest --watch --noStackTrace --passWithNoTests --coverage --runInBand",
    "test:unit": "jest --watchAll -c jest-unit-config.js --passWithNoTests --coverage --runInBand",
    "test:integration": "jest --watchAll -c jest-integration-config.js --passWithNoTests --runInBand",
    "test:staged": "jest"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelf/jest-mongodb": "^1.2.3",
    "jest": "^26.6.3",
    "standard": "^16.0.3",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.6.4",
    "validator": "^13.5.2"
  }
}
