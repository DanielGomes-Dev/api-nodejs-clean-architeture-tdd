{
  "name": "api-nodejs-clean-architecture-tdd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch --noStackTrace --passWithNoTests --coverage",
    "test:unit": "jest --watchAll -c jest-unit-config.js --passWithNoTests",
    "test:integration": "jest --watchAll -c jest-integration-config.js --passWithNoTests",
    "test:staged": "jest"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3",
    "standard": "^16.0.3"
  },
  "dependencies": {
    "validator": "^13.5.2"
  }
}
